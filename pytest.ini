[pytest]
testpaths = tests
addopts = --cov=src --cov-report=term-missing --cov-report=html:htmlcov -v
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Qt UI deprecated and removed — no --ignore flags needed
